import styled from 'styled-components/native';

export default styled.TextInput`
    padding: 10px;
    background: #fff;
    margin-bottom: 20px;
    border: ${props => props.border};
    border-radius: 5px;
`;
